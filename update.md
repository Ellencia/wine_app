# 변경 이력

## 2026-08-25
- 홈 필터 모바일 UX 수정: 가로 스크롤 제거(칩 줄바꿈), 품종은 상위 6개 + "품종 더보기" 패널(빈도 표시)로 축약, 패널·상세 태그에서 고른 품종은 선택 칩 한 칸에만 표시
- 프로필 상태(estimated/verified)·품종 태그 컬럼 추가, 품종 표기 정규화(wine_utils.py, 64개 태그), 홈 타입×품종 필터(URL 파라미터 공유), 상세에 "추정치·검수 전" 라벨과 품종 태그, 퀴즈 동점 시 검수 와인 우선
- 와인 용어사전 22개(등급·산지·양조·당도·시음·재배) + 이름·테크시트·소개문 용어 자동 링크, 헤더 메뉴 추가
- 협업 규칙 문서화: AGENTS.md(단일 출처)·CLAUDE.md — fetch 선행, 파일 명시 add, 엑셀→JSON 재생성 필수, 가격 정보 금지, 배포 SHA 확인
- 데이터 파이프라인 강화(Codex 작업 반영): 스타일 점수 정수 검증(2.5·불리언 거부), 생산자 id 중복 검사, openpyxl==3.1.5 고정(requirements.txt), CI에서 엑셀→JSON 재생성 후 커밋본과 비교해 불일치 시 배포 중단
- 올빈와인 카탈로그(와인리스트 2026 N°3, 51쪽 스캔) 130종·생산자 30곳 전사 — 공급가 제외. data/wines.xlsx 마스터 + scripts/build_data.py -> src/data/*.json 파이프라인으로 전환
- 보틀 이미지 130개를 카탈로그 페이지에서 레이아웃 기반 자동 검출로 크롭(260dpi, 흰 배경 투명화), 어두운 배경 페이지는 수동 좌표
- 앱 갱신: 브랜드 ALLVIN WINE, 국가별 컬렉션 + 타입 필터 칩, 생산자 목록·상세 페이지, 상세에 Vivino 점수·링크와 뱃지·수상 표시, 퀴즈 후보를 실제 와인으로 교체(디저트 와인 선택지 추가), 스타일 프로필 초기값 규칙 산출
- 아르헨티나 샘플 와인·SVG 삭제, OG 기본 이미지를 실제 보틀로 재생성

## 2026-08-12
- 취향 퀴즈 로직 개선: 축 시작점 3·음수 가점(싫음 표현)·중립 답은 비교 제외·당도/타닌 가중치, 결과에 취향 요약 문장 + 4축 프로필 표시, 공유 URL에 프로필 포함
- 샘플 와인 7종 추가(총 10종): JiJiJi, Cara Sur Criolla, Chacra Barda, Riccitelli Semillón, Colomé Torrontés, Torrontés Brutal, Malamado — 품종·산지·도수 웹 검증, 보틀 SVG 7종, 퀴즈 4가지 취향 경로가 서로 다른 와인으로 갈라지는 것 확인
- 부가기능 7종 추가: PWA 홈 화면 설치(manifest+아이콘), OG 공유 미리보기, 퀴즈 결과 URL 공유(Web Share), 테크시트 인쇄 CSS, 404 페이지, 사이트맵, 칵테일 wineId 빌드 검증, 와인 상세 이전/다음 네비
- GitHub Pages 배포 완료 — https://ellencia.github.io/wine_app/ (repo Ellencia/wine_app, MIT LICENSE, Actions 자동 배포)
- 하위 경로 배포 대응: astro.config에 site/base 설정, 내부 링크·이미지 전부 url() 헬퍼로 base 프리픽스 처리
- Astro 7 정적 웹앱 프로토타입 첫 구축 — 카탈로그·테크니컬 시트·칵테일 레시피·취향 퀴즈 4기능 완성, IGNEA풍 디자인 재현
- 목업의 와인 3종을 샘플 데이터로 입력, 보틀은 SVG 플레이스홀더 일러스트
- 빌드 8페이지 통과, 390px 모바일 뷰포트에서 전 페이지 스크린샷·퀴즈 클릭 구동 검증 (가로 오버플로 없음)
