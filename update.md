# 변경 이력

## 2026-09-03
- CI에 타입 검사(astro check) 단계 추가 — 기존 타입 오류 1건(hidden 속성 boolean 좁히기) 수정, 퀴즈 define:vars 스크립트에 is:inline 명시, 배포 동시 실행 방지 concurrency 설정
- 데이터 파이프라인 2차 강화(build_data.py): 가격·재고류 컬럼이 엑셀에 유입되면 빌드 즉시 중단, 컬럼을 위치가 아닌 헤더명으로 매핑(순서 변경·삽입에 안전), id 공백 행을 조용히 건너뛰지 않고 오류 처리, 도수(0~25)·Vivino(0~5)·정렬 중복·id 슬러그·뱃지 화이트리스트 검증 추가
- 보틀 이미지 WebP 서빙: 페이지 img를 WebP로 전환(전송량 15MB -> 1.9MB, PNG는 OG 이미지용 유지), npm run images 스크립트 + wine-image-dims.json으로 width/height 부여(레이아웃 밀림 방지), 홈·생산자 카드 중복을 WineCard 공용 컴포넌트로 통합
- SEO 기본기: 전 페이지 canonical 태그, robots.txt, 홈·생산자·칵테일 h1 추가, 퀴즈·칵테일 페이지별 description, 와인 상세 Product(가격 미포함)·생산자 Organization 구조화 데이터(JSON-LD)
- 퀴즈 접근성: 문항 전환 시 새 질문으로 포커스 이동, 진행 표시 aria-live, "이전 질문으로" 버튼 신설(답 목록 재생 방식이라 물린 답의 영향이 정확히 사라짐), 결과 화면 포커스 이동; 홈 필터 칩에 aria-pressed·그룹 라벨
- 표기 정리: 와인 상세 종류를 카탈로그 표기(typeLabel)로 통일(헤더·테크시트 불일치 39종 해소), 산지=국가 와인의 "프랑스, 프랑스" 중복 표기 방어, 칵테일 영상 자리의 개발자용 안내를 "영상 준비 중입니다"로 교체

## 2026-09-02
- 홈 상단 타입·품종 필터의 PC 가로 탐색 보완: 마우스 드래그와 휠 스크롤 지원, 드래그 종료 시 의도치 않은 필터 클릭 방지
- 홈에 와인명·생산자·산지·품종 통합 검색 추가: 기존 타입·품종 필터와 함께 적용되며 검색어를 URL로 공유·복원
- 링크·버튼·입력창에 키보드 Tab 포커스 표시 추가, 검색 결과 수를 스크린 리더에 안내

## 2026-09-01
- 폰테조파 바르디: p22 임베디드 원본(840x1335, 뱃지 없는 스튜디오 컷)을 찾아 누끼로 교체 — 종횡비 필터(1.6)에 걸려 누락됐던 이미지
- 폴백 이미지 10개 재처리: 테두리 flood-fill 배경 제거 + 섬 성분 필터로 뱃지·텍스트 조각 제거 (바르디 흰 사각형 해소, 박스+병 세트 보존)
- 앱 보틀 이미지 130개를 raw_bottles 기반으로 재생성(누끼 123 + rembg AI 누끼 6 + 불투명 1) — 잘림·스티커 문제 해소, 폴더 23MB->16MB, OG 기본 이미지 재생성

## 2026-08-27
- 원본 보틀 이미지 라이브러리 raw_bottles/ 추출 — 임베디드 300dpi 누끼 114개(알파 포함) + 배경 포함 폴백 크롭 16개, git 제외
- 카탈로그 JSON 공개 엔드포인트 /data/wines.json 추가 — 내부 매출 통계 도구(wine_stats)가 와인명 매칭용으로 내려받음, 가격 정보 없음

## 2026-08-25
- 홈 필터 모바일 UX 수정: 타입·품종 각 한 줄 가로 스크롤(칩 축소), 품종은 상위 6개 + 스크롤 밖 고정 "더보기" 버튼 -> 빈도순 패널, 고른 품종은 "전체" 옆 선택 칩 한 칸에 표시
- 프로필 상태(estimated/verified)·품종 태그 컬럼 추가, 품종 표기 정규화(wine_utils.py, 64개 태그), 홈 타입×품종 필터(URL 파라미터 공유), 상세에 "추정치·검수 전" 라벨과 품종 태그, 퀴즈 동점 시 검수 와인 우선
- 와인 용어사전 22개(등급·산지·양조·당도·시음·재배) + 이름·테크시트·소개문 용어 자동 링크, 헤더 메뉴 추가
- 협업 규칙 문서화: AGENTS.md(단일 출처)·CLAUDE.md — fetch 선행, 파일 명시 add, 엑셀→JSON 재생성 필수, 가격 정보 금지, 배포 SHA 확인
- 데이터 파이프라인 강화(Codex 작업 반영): 스타일 점수 정수 검증(2.5·불리언 거부), 생산자 id 중복 검사, openpyxl==3.1.5 고정(requirements.txt), CI에서 엑셀→JSON 재생성 후 커밋본과 비교해 불일치 시 배포 중단
- 올빈와인 카탈로그(와인리스트 2026 N°3, 51쪽 스캔) 130종·생산자 30곳 전사 — 공급가 제외. data/wines.xlsx 마스터 + scripts/build_data.py -> src/data/*.json 파이프라인으로 전환
- 보틀 이미지 130개를 카탈로그 페이지에서 레이아웃 기반 자동 검출로 크롭(260dpi, 흰 배경 투명화), 어두운 배경 페이지는 수동 좌표
- 앱 갱신: 브랜드 ALLVIN WINE, 국가별 컬렉션 + 타입 필터 칩, 생산자 목록·상세 페이지, 상세에 Vivino 점수·링크와 뱃지·수상 표시, 퀴즈 후보를 실제 와인으로 교체(디저트 와인 선택지 추가), 스타일 프로필 초기값 규칙 산출
- 아르헨티나 샘플 와인·SVG 삭제, OG 기본 이미지를 실제 보틀로 재생성

## 2026-08-12
- 취향 퀴즈 로직 개선: 축 시작점 3·음수 가점(싫음 표현)·중립 답은 비교 제외·당도/타닌 가중치, 결과에 취향 요약 문장 + 4축 프로필 표시, 공유 URL에 프로필 포함
- 샘플 와인 7종 추가(총 10종): JiJiJi, Cara Sur Criolla, Chacra Barda, Riccitelli Semillón, Colomé Torrontés, Torrontés Brutal, Malamado — 품종·산지·도수 웹 검증, 보틀 SVG 7종, 퀴즈 4가지 취향 경로가 서로 다른 와인으로 갈라지는 것 확인
- 부가기능 7종 추가: PWA 홈 화면 설치(manifest+아이콘), OG 공유 미리보기, 퀴즈 결과 URL 공유(Web Share), 테크시트 인쇄 CSS, 404 페이지, 사이트맵, 칵테일 wineId 빌드 검증, 와인 상세 이전/다음 네비
- GitHub Pages 배포 완료 — https://ellencia.github.io/wine_app/ (repo Ellencia/wine_app, MIT LICENSE, Actions 자동 배포)
- 하위 경로 배포 대응: astro.config에 site/base 설정, 내부 링크·이미지 전부 url() 헬퍼로 base 프리픽스 처리
- Astro 7 정적 웹앱 프로토타입 첫 구축 — 카탈로그·테크니컬 시트·칵테일 레시피·취향 퀴즈 4기능 완성, IGNEA풍 디자인 재현
- 목업의 와인 3종을 샘플 데이터로 입력, 보틀은 SVG 플레이스홀더 일러스트
- 빌드 8페이지 통과, 390px 모바일 뷰포트에서 전 페이지 스크린샷·퀴즈 클릭 구동 검증 (가로 오버플로 없음)
